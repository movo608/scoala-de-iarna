import React, { Component } from 'react'
import { connect } from 'react-redux'

class Blog extends Component {
	render() {
		return (
			<div></div>
		);
	}
}

export default connect(undefined, undefined)(Blog);